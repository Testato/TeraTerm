TTXttyrec -- 端末画面レコーダー

機能:
  端末の画面表示を録画し、ファイルに保存します。
  コントロールメニューのRecordを選択すると保存ダイアログが出るので、
  ファイルを指定すると録画が始まります。
  再度Recordを選択すると録画を終了します。

  保存されたデータはttyrec(http://0xcc.net/ttyrec/)と同じ形式ですので、
  ttyplayで再生する事ができます。

解説:
  TCP接続の通信内容をフックするサンプルプラグインです。
  Precvをフックして、現在の時刻情報と受信データをttyrecの形式でファイルに
  書き込んでいるだけです。

  Windowsには gettimeofday() がないので、関数を書き起こしています。

バグ:
  ・シリアル接続には使用できません。
  ・録画するのみで、再生する事はできません。

ToDo:
  ・シリアルポートに対応する (TeraTerm側のプラグイン関連の機能追加が必要)
  ・録画したデータの再生に対応する。
